# Border checking of ball
    # if ball.ycor()  > 290:
    #     ball.sety(290)
    #     ball.dy *= -1

    # if ball.ycor()  < -290:
    #     ball.sety(-290)
    #     ball.dy *= -1

    # if ball.xcor() > 390:
    #     ball.goto(0 ,0)
    #     ball.dx *= -1