#include <stdio.h>
#include <string.h>

int main()
{
    // int a[10];
    // printf("%d %d\n", a, &a);
    // printf("%d\n", &a[0]);
    // printf("%d %d\n", sizeof(a), sizeof(&a));
    // printf ("%d %d\n", a+1, &a+1);

    return 0;
}
