#include <stdio.h>

int main()
{
    FILE * fpw, * fpr;

    // fpr = fopen("this.txt", "r");

    // ***reading a character from a file***
    // char c;
    // c = fgetc(fpr);
    // printf("%c\n", c);
    // c = fgetc(fpr);
    // printf("%c\n", c);

    // ***reading a string from a file***
    // char str[10];
    // fgets(str, 10, fpr);
    // printf("%s", str);

    // ---------------------------------------------------

    // fpw = fopen("that.txt", "w");

    // ***printing a character to a file***
    // fputc('t', fpw);

    // ***printing a string to a file***
    // fputs("This is a string", fpw);

    return 0;
}
